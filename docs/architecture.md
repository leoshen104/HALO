# HALO research scaffold architecture

This repository is transitioning from a monolithic Streamlit prototype (`app.py`) to a modular package under `halo/`.

Current scaffold includes:
- deterministic config loader
- seed manager
- provenance manifest generator
- canonical signal naming helpers
- VitalDB integration stubs (library + API fallback)

Safety posture: non-autonomous, educational/research use only.
